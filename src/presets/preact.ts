import { ImportsMap } from '../types'

export default <ImportsMap>({
  preact: [
    'useState',
    'useCallback',
    'useMemo',
    'useEffect',
    'useRef',
    'useContext',
    'useReducer',
  ],
})
