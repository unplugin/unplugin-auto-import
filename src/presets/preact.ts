import type { ImportsMap } from '../types'

export default <ImportsMap>({
  preact: [
    'useState',
    'useCallback',
    'useMemo',
    'useEffect',
    'useRef',
    'useContext',
    'useReducer',
  ],
})
