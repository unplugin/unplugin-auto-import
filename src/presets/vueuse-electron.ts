import type { ImportsMap } from '../types'

export default <ImportsMap>({
  '@vueuse/electron': [
    'useIpcRenderer',
    'useIpcRendererInvoke',
    'useIpcRendererOn',
    'useZoomFactor',
    'useZoomLevel'
  ],
})
